<x-app-layout><x-slot name="header"><h2 class="font-semibold text-xl">Branches</h2></x-slot><div class="py-12"><div class="max-w-4xl mx-auto sm:px-6 lg:px-8 space-y-6">@if(session('status'))<div class="p-4 bg-green-50 text-green-800 rounded">{{ session('status') }}</div>@endif<div class="bg-white p-6 shadow-sm sm:rounded-lg"><form method="POST" action="{{ route('branches.store') }}" class="grid grid-cols-1 sm:grid-cols-4 gap-3">@csrf<input name="code" placeholder="Code e.g. VITA1" class="border rounded p-2" required><input name="name" placeholder="Name" class="border rounded p-2" required><input name="location" placeholder="Location" class="border rounded p-2"><button class="px-4 py-2 bg-gray-800 text-white rounded">Add</button></form></div><div class="bg-white shadow-sm sm:rounded-lg"><table class="min-w-full text-sm"><thead class="bg-gray-50 text-left"><tr><th class="p-3">Code</th><th class="p-3">Name</th><th class="p-3">Location</th></tr></thead><tbody>@foreach ($branches as $b)<tr class="border-t"><td class="p-3">{{ $b->code }}</td><td class="p-3">{{ $b->name }}</td><td class="p-3">{{ $b->location }}</td></tr>@endforeach</tbody></table></div></div></div></x-app-layout>
